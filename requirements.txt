# Direct dependencies of the time-complexity LLM pipeline (documentation only).
#
# To actually install, use the pinned lock instead -- Unsloth breaks between releases,
# and every experiment must run on the same stack for the results to be comparable:
#   pip install -r requirements-lock.txt
#
# Unsloth pulls its own compatible stack (torch, transformers, trl, peft,
# bitsandbytes, xformers, datasets, ...). On Colab a CUDA-enabled PyTorch is
# already present; on a bare machine install a CUDA build of torch first.
# Python 3.10-3.12 and an NVIDIA GPU are required.
#
# For the exact versions verified to work end-to-end, see requirements-lock.txt.

unsloth
unsloth_zoo
hf_transfer
